{
  "name": "template",
  "version": "0.0.0",
  "description": "A repository for Zhycorp's project base",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "npm run lint && npm run compile",
    "compile": "tsc --build tsconfig.json",
    "lint": "eslint . --ignore-path .gitignore --ext .ts",
    "lint:fix": "eslint . --ignore-path .gitignore --ext .ts --fix",
    "start": "node --experimental-specifier-resolution=node dist",
    "start:dev": "rimraf ./dist && npm run compile && node --experimental-specifier-resolution=node --no-warnings dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zhycorp/template.git"
  },
  "keywords": [
    "js",
    "typescript"
  ],
  "author": "Zhycorp <contact@zhycorp.org>",
  "license": "AGPL-3.0",
  "bugs": {
    "url": "https://github.com/zhycorp/template/issues"
  },
  "homepage": "https://zhycorp.org/",
  "engines": {
    "node": ">=16.6.0",
    "npm": ">=7.0.0"
  },
  "eslintConfig": {
    "extends": "@hazmi35/eslint-config/typescript",
    "ignorePatterns": "dist/*"
  },
  "devDependencies": {
    "@hazmi35/eslint-config": "^8.3.0",
    "@types/node": "^17.0.23",
    "@typescript-eslint/eslint-plugin": "^5.27.0",
    "@typescript-eslint/parser": "^5.27.0",
    "eslint": "^8.16.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.7.2"
  }
}
